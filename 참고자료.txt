baekjoon onlive judge : https://www.acmicpc.net/ (코딩테스트)
형변환
1. String to Int
int i_num = Integer.parseInt(s_num);
String.valueOf(ch); // _____.valueOf : 형변환
->_____. : ex) String, int, bool....

2. Int to String
s_num = Integer.toString(i_num);

3. 배열 출력
Arrays.deepToString : int 형
Arrays.toString : string 형

4. 문자열 자르기
String[] array = str.split(" ");
String str = str.substring(n) : n 인덱스
->char[] ch = phone_number.toCharArray();
  ->한글 자씩 ch에 저장

for(int i = 0; i < 500; i++) {
	    	  if(num % 2 == 0) {
	    		  num = num/2;
	    	  }else {
	    		  num = num*3 + 1;
	    	  }
	    	  answer ++;
	    	  if(num == 1) break;
	      }
	      if(answer == 500) {
	    	  answer = -1;
	      }

5. 배열정리(sort)
 - 오름차순 : Arrays.sort(x);
 - 내림차순 : Arrays.sort(X, Collections.reverseOrder());
 - 내림차순 : reverseArrayInt(i); -> int형

6. 제곱근(math 함수 사용)
-Math.sqrt(x); 	// 제곱근
-Math.pow(count, n);	//n승

7. 대소문자 변환
 - s=s.toUpperCase();
//모두 대문자로 바꾸기
s=s.toLowerCase();
/모두 소문자로 바꾸기

8. 시스템 시간 측정
 - System.currentTimeMillis();

9. str.charAt(n) : str의 n번째 인수 추출

10. 배열 타입 변환
 - list to array
	String[] array = arrayList.toArray(new String[arrayList.size()]);
 - array to list
	ArrayList<String> arrayList = new ArrayList<>(Arrays.asList(array));

 - List<Integer> intArr = Arrays.stream(arr).boxed().collect(Collectors.toList());

11. 실행시간
long start = System.currentTimeMillis();
long finish = System.currentTimeMillis();
long time = finish - start;

12 진수 변환
int a = Integer.bitCount(n); // 2진수 변환
String a2 = Integer.toBinaryString(num);  // 10진수 -> 2진수
String a8= Integer.toOctalString(num);    // 10진수 -> 8진수
String a16 = Integer.toHexString(num);    // 10진수 -> 16진수

String a2 = Integer.toBinaryString(num);
String a8= Integer.toOctalString(num);
String a16 = Integer.toHexString(num);